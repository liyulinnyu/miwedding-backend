Haven't done user authorization, hash password and so on. But data transfer function could be used.

Using Express.js, graphql, jwt, mongoose and so on.  

'cd' to the local folder, and run 'npm install' -> 'npm start'